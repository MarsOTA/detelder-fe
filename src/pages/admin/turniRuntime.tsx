import Turni from "./turni";

/**
 * Wrapper mantenuto per compatibilità con il routing del prototipo.
 * La logica del filtro data vive ora direttamente in turni.tsx tramite
 * il componente condiviso DetelderDateRangePicker.
 */
export default Turni;
